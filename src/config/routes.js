const routes = {
    home: '/',
    dashboard: '/dashboard',
    products: '/products',
    product_categoty: '/products/:categoryName',
    product_detail: '/detail/:id',
    cart: '/cart/:id',
    history: '/history/:id',
};

export default routes;
